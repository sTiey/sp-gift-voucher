# แผนระยะสั้น — แก้ผลที่เจอบนมือถือจริง

> ตั้งจากผลทดสอบจริงบน iPhone (2026-08-18) ที่ `https://stiey.github.io/sp-gift-voucher/`
> ไท้สั่ง: ทำให้เหมือนเดิม · อย่ามองข้ามประสิทธิภาพ · ทำทีละอย่าง · ข้อ 3 สำคัญมาก
>
> **กติกาของแผนนี้: ทุกข้อต้องมี "วิธีพิสูจน์" ที่ไม่ใช่การมองด้วยตา**
> เพราะบั๊กสามตัวที่เจอมาทั้งหมดในโปรเจกต์นี้ ไม่มีตัวไหนฟ้อง error เลยสักตัว

---

## ข้อ 1 · ผิวทองต้องขึ้นบน iPhone  ⬅ ทำอยู่

**อาการ** ตัวเลขเป็นเกล็ดสีเงิน ไม่ใช่ทอง
**หลักฐาน** ในคลิป ขอบนูนยังเป็นทองน้ำตาล แต่เนื้อในเป็นเงิน — สองส่วนนี้วาดคนละกลไก
แปลว่า **การผสมสีระหว่างชั้น (`background-blend-mode`) ไม่ทำงานเมื่อใช้คู่กับการฉลุตามรูปตัวอักษร**
ชั้นบนสุด (เกล็ดสีเทา) จึงทาทับพื้นทองจนหมด

**ทำไมดักล่วงหน้าไม่ได้** ถาม `CSS.supports` แล้ว Safari ตอบว่ารองรับ — มันพังเฉพาะตอนใช้คู่กัน
ไม่มี error ไม่มีคำเตือน

**ทางแก้** เลิกใช้การผสมสีทั้งหมด → แปลงพื้นผิวเป็น **ภาพโปร่งใส (RGBA)**
วางทับด้วยอัลฟาตรง ๆ เหมือนที่ทำเนื้อกระดาษสำเร็จมาแล้ว · ได้ผลเท่ากันทุกเบราว์เซอร์

**วิธีพิสูจน์ว่า "เหมือนเดิม"**
เรนเดอร์ใบเดิมกับใบใหม่ที่มุมแสงเดียวกันหลายมุม แล้ว **วัดความต่างของภาพเป็นตัวเลข**
ไม่ใช่ดูด้วยตา · ปรับค่าอัลฟาจนความต่างต่ำกว่าที่ตาแยกออก

**วิธีพิสูจน์เรื่องประสิทธิภาพ** วัด A/B แบบเดิม (โคลนการ์ด 30 ใบ สลับเปิด/ปิด 9 รอบ)
ตัดการผสมสีออก = งานน้อยลง ต้องไม่ช้าลง

---

## ข้อ 2 · ไจโรสโคป

**อาการ** เอียงเครื่องแล้วไม่มีอะไรขยับ · ลากนิ้วแล้วขยับปกติ
**สาเหตุที่ตั้งไว้** ไลบรารีสมัครฟังการเอียง **ตั้งแต่ตอนโหลดหน้า** ซึ่ง iPhone ยังไม่อนุญาต
พอผู้ใช้แตะแล้วได้สิทธิ์ ไลบรารีไม่ได้กลับไปสมัครใหม่ → ไม่เคยได้ยินการเอียงเลย

**ทางแก้**
1. **ขออนุญาตให้เสร็จก่อน แล้วค่อยติดตั้งไลบรารี** (ไม่ใช่ติดตั้งก่อนแล้วขอทีหลัง)
2. **ดักฟังการเอียงด้วยตัวเองอีกทางหนึ่ง** ไม่ฝากไว้กับไลบรารีอย่างเดียว
3. หน้าเว็บต้อง **บอกสถานะออกมาตรง ๆ** ว่าอยู่ขั้นไหน: ยังไม่ขอ / ขอแล้วถูกปฏิเสธ /
   ได้สิทธิ์แล้วแต่ยังไม่มีสัญญาณเข้ามา / มีสัญญาณแล้วกี่ครั้ง

**วิธีพิสูจน์** นับจำนวนสัญญาณการเอียงที่เข้ามาจริง แล้วส่งกลับมาในรายงาน (ข้อ 3)
"ไม่มี error" ไม่นับว่าผ่าน

---

## ข้อ 3 · หน้าทดสอบบนมือถือ + ระบบวัดผลอัตโนมัติ

**ปัญหาตอนนี้** หน้าเทสเป็นหน้าสำหรับดูบนคอม — ใบแนวยาวโดนตัดข้าง ปุ่มเล็ก
และ **ทุกอย่างที่อยากรู้ ต้องให้ไท้อธิบายเป็นคำพูด** ซึ่งช้าและตกหล่น

**จะทำ**

**ก · หน้าเทสสำหรับมือถือโดยเฉพาะ**
- ทีละใบเต็มจอ ไม่ต้องเลื่อน
- ใบแนวยาว: หมุนให้พอดีจอแนวตั้ง หรือบอกให้หมุนเครื่อง — ไม่ปล่อยให้โดนตัด
- บอกผู้ทดสอบเป็นขั้น ๆ ว่าต้องทำอะไร (แตะ → เอียง → ดูอะไร)

**ข · เก็บผลอัตโนมัติ แล้วส่งกลับมาได้ด้วยการกดปุ่มเดียว**
เก็บ: รุ่นเครื่อง/เบราว์เซอร์ · ขนาดจอ · แรม/คอร์ที่เครื่องบอก · สถานะไจโรและจำนวนสัญญาณ ·
ความลื่นตอนเอียง (เวลาต่อเฟรม ค่ากลางและค่าแย่สุด) · เวลาโหลด · น้ำหนักที่โหลดจริง ·
สถานะ "ลดแอนิเมชัน" · ผลตรวจว่าผิวทองขึ้นถูกไหม
แล้วมีปุ่ม **"คัดลอกผลทดสอบ"** ปุ่มเดียว → ไท้วางในแชต → ผมได้ตัวเลขจริงทั้งชุด

**ทำไมไม่ส่งขึ้นเซิร์ฟเวอร์เอง** เว็บนี้เป็นไฟล์นิ่ง ไม่มีหลังบ้าน · การส่งข้อมูลออกเองยัง
หมายถึงต้องมีที่เก็บและมีเรื่องความเป็นส่วนตัวตามมา · **กดคัดลอกแล้ววางเอง เร็วกว่าและไม่มีอะไรรั่ว**

---

## ลำดับ

1. ข้อ 1 (ผิวทอง) → วางเทียบให้ไท้ดูบนคอม
2. ข้อ 2 (ไจโร)
3. ข้อ 3 (หน้าเทส + ระบบวัดผล)
4. ไท้เทสรอบเดียวบนมือถือ → กดปุ่มคัดลอก → ส่งกลับมา → ผมอ่านตัวเลขจริงทั้งชุด

**ยังไม่ commit จนกว่าแต่ละข้อจะผ่านสายตาไท้** · จุดเซฟล่าสุดคือ `e28c72c`

---

# ข้อ 1 — เสร็จแล้ว รอไท้ตรวจ (2026-08-19)

**ยืนยันสาเหตุ** จากคลิปบนเครื่องจริง: ขอบนูนยังเป็นทอง เนื้อในเป็นเงิน
= การผสมสีระหว่างชั้นไม่ถูกใช้ ชั้นบนสุด (เกล็ดสีเทา) ทาทับพื้นทองจนหมด

**ทางที่ตัดทิ้งเพราะคำทักของไท้** — เดิมจะลอง "ซ้อนชั้นลูกแล้วใช้ mix-blend-mode"
ไท้เล่าว่าเคยแปลงงานให้รันบน Safari ได้แล้วเครื่องทำงานหนักมากทั้งที่ตาเห็นแค่มุมโค้ง
นั่นคือกับดักเดียวกันเป๊ะ — ชั้นแบบนั้นบังคับให้เครื่องกันหน่วยความจำภาพเต็มขนาดไว้
ชั้นละก้อนแล้ววาดใหม่ทุกเฟรม ยิ่งการ์ดหลายใบยิ่งคูณ → **ไม่ลอง**

## สิ่งที่ทำจริง

เลิกใช้การผสมสีทั้งหมด แปลงพื้นผิวเป็นภาพโปร่งใสวางทับตรง ๆ
**กุญแจคือแยก "โทนเฉลี่ย" ออกจาก "เม็ดผิว"** — เอาโทนเฉลี่ยไปอบไว้ในไล่เฉดทองพื้น
(ฟรี) เหลือให้ภาพแบกแค่เม็ดผิว ตรงที่เท่าค่าเฉลี่ยคือโปร่งใสสนิท
ส่วนที่สีตายตัวยังกดไล่เฉดกับความอิ่มสีลง — วัดค่านั้นแล้วแก้คืนที่ไล่เฉดพื้น ซึ่งก็ฟรี

## วัดผลเทียบกับใบที่อนุมัติ (เรนเดอร์จริง 5 มุมแสง)

| | ผล |
|---|---|
| เม็ดผิวฟอยล์ | **100%** |
| ความสว่าง | ต่างกัน **1.2/255** |
| ความอิ่มสี | **92%** |
| ไล่เฉดทองบน-ล่าง | **83%** |
| ต่างเฉลี่ยทั้งภาพ | 10.3/255 (4%) |

ใบทุกแบบตรวจแล้ว: % · จำนวนเงิน · ส่งฟรี · กระทัดรัด · และใบที่ใช้แล้ว
(ใบที่ใช้แล้วต้องแก้พื้นให้อมฟ้า เพราะแผ่นผิวเป็นสีทองในตัว — ไม่งั้นออกมาเป็นทองซีด
ตอนนี้ตรงกันทุกช่องสีในระยะ 1 หน่วย)

## ประสิทธิภาพ (วัด A/B สลับกัน 5 รอบ ตัวเลข 160 ตัวขยับทุกเฟรม)

| | เวลาต่อเฟรม (กลาง) | เฟรมที่แย่สุด 5% |
|---|---|---|
| เดิม (ผสมสี) | 83.3 ms | 150 ms |
| ใหม่ (โปร่งใส) | 66.7–83.2 ms | **133 ms** |

**ไม่เคยช้ากว่าเดิมสักรอบ** และเฟรมที่แย่สุดดีขึ้นทุกรอบ
เครื่องมือวัดอยู่ที่ `demo/_perf-foil.html` (เปิดแล้ววัดเอง)

## ต้นทุนที่ต้องบอกตรง ๆ

ไฟล์พื้นผิว **144 KB → 299 KB (+155 KB)** โหลดครั้งเดียวแล้วใช้ร่วมทุกใบ
ลองย่อแล้วไม่ได้ — ความทึบเป็นรูปตัว V รอบค่ากลาง ย่อเมื่อไหร่เม็ดผิวหายทันที
(วัดได้ 100% → 70% ที่ 256px) · ชั้นแผนที่ห้องย่อได้เพราะมีรายละเอียดละเอียดแค่ 4%
จึงเหลือ 7 KB จาก 42 KB แล้ว

---

# ข้อ 2 — เสร็จแล้ว รอยืนยันบนเครื่องจริง (2026-08-19)

**เจอสามเรื่องซ้อนกัน ไม่ใช่เรื่องเดียว**

1. **ไลบรารีสมัครฟังการเอียงตั้งแต่ตอนโหลดหน้า** ซึ่งคือก่อนได้สิทธิ์เสมอ
   (iOS ขอสิทธิ์ได้เฉพาะตอนผู้ใช้แตะ ซึ่งเกิดทีหลัง) แล้วไม่เคยกลับไปสมัครใหม่
2. **ผลของการขอสิทธิ์ถูกทิ้ง** → ถูกปฏิเสธ / เครื่องปิดเซนเซอร์ / ไม่มีเซนเซอร์
   หน้าตาเหมือนกันหมดคือเงียบสนิท แยกไม่ออกว่าพังตรงไหน
3. **ไลบรารีไม่รู้จักการหมุนจอ** — ถือแนวนอนแล้วแกนสลับ ใบจะเอียงผิดทาง
   (สำคัญมากกับใบแนวยาวที่ต้องดูแนวนอนในข้อ 3)

**ทำใหม่ทั้งท่อ** ปิดไจโรของไลบรารีทิ้ง เขียนศูนย์กลางเอง:
ขอสิทธิ์ให้ผ่านก่อนแล้วค่อยฟัง · หมุนแกนตามมุมจอ · **จำท่าถือปกติของคนใช้**
(ไม่งั้นคนถือมือถือเอียง 45° การ์ดจะค้างสุดทางตั้งแต่ยังไม่ขยับ) ·
ฟัง `deviceorientationabsolute` ควบไว้ด้วยเพราะแอนดรอยด์บางรุ่นส่งมาทางนั้นทางเดียว ·
**นับสัญญาณไว้ให้ตรวจได้** · ใช้ตัวฟังตัวเดียวขับได้ทุกใบ ไม่ใช่ใบละตัว

ป้อนเข้าไลบรารีเป็น mousemove ปลอม = ได้ทางเดินเดียวกับเมาส์ซึ่งพิสูจน์แล้วว่าทำงาน
ไม่ต้องเขียนโค้ดขยับใบซ้ำ และไม่ต้องแตะข้างในไลบรารีเลย

**หน้าเว็บบอกสถานะตรง ๆ แล้ว**: ยังไม่ขอ / ถูกปฏิเสธ (พร้อมบอกวิธีเปิด) /
ได้สิทธิ์แล้วแต่ยังไม่มีสัญญาณ / ทำงานอยู่ + จำนวนครั้ง + องศาที่เอียงอยู่ /
เตือนถ้าไม่ได้เปิดผ่าน https

**พิสูจน์แล้ว 11 ข้อ** — `demo/_check-gyro.html` ยิงสัญญาณเซนเซอร์ปลอมเข้าไปทาง event
เดียวกับของจริง แล้ววัดว่าใบเอียงจริงและผิวฟอยล์ขยับจริง รวมถึงกรณีหมุนจอแนวนอน
และกรณีเครื่องไม่มีเซนเซอร์ · การ์ดยังเรนเดอร์เหมือนเดิมเป๊ะ (ต่างกัน 0.000/255)

⚠️ **สิ่งที่ยืนยันบนคอมไม่ได้** คือสิทธิ์บน iOS ผ่านจริงไหม และเซนเซอร์ส่งค่ามาจริงไหม
→ ต้องเครื่องไท้เท่านั้น ซึ่งคือหน้าที่ของข้อ 3
# ข้อ 3 — เสร็จแล้ว รอไท้เทสบนเครื่องจริง (2026-08-19)

**หน้าทดสอบ** `demo/mobile-test.html` — ทีละใบเต็มจอ เลื่อนไม่ได้ บอกเป็นขั้น 4 ขั้น
ปุ่ม "เริ่มทดสอบ" ทำหน้าที่ขอสิทธิ์ไจโรไปในตัว (iOS ขอได้เฉพาะตอนแตะ)
ระหว่างดูบัตรมีแถบสถานะไจโรเดินให้เห็นสด ๆ ว่ารับสัญญาณมากี่ครั้ง เอียงอยู่กี่องศา

**บัตรแต่ละทรงขอท่าถือของตัวเอง** ใบยาว 2.6:1 ขอแนวนอน · ใบกระทัดรัดขอแนวตั้ง
ไม่ปล่อยให้โดนบีบจนดูผิวทองไม่ออก · และการหมุนเครื่องกลางทาง**ได้ทดสอบไปในตัว**
ว่าไจโรสลับแกนตามการหมุนจอถูกไหม (ซึ่งคือบั๊กข้อ 3 ที่แก้ไปในข้อ 2)

**ระบบเก็บผล** `src/ui/device-report.js` (แยกเป็นโมดูล 5 หน้าใช้งานจริงเอาไปใช้ต่อได้)
เก็บ: รุ่นเครื่อง/เบราว์เซอร์/เวอร์ชัน · ขนาดจอ · แรม/คอร์/ความเร็วเน็ต · สถานะ "ลดแอนิเมชัน" ·
สถานะไจโร + จำนวนสัญญาณ + ทางที่ใช้ · เวลาต่อเฟรมตอนเอียง (ค่ากลาง/แย่สุด/สัดส่วนที่สะดุด) ·
เวลาโหลด · น้ำหนักที่โหลดจริงแยกจากที่หยิบจากแคช + ไฟล์ที่หนักสุด 5 อันดับ

**ตรวจผิวทองอัตโนมัติ** — ตอนนี้ผิวทองพึ่งช่องความโปร่งใสของไฟล์ภาพล้วน ๆ
ถ้าเบราว์เซอร์เก่าอ่านไฟล์ได้แต่**ทิ้งช่องความโปร่งใส** ผิวจะกลายเป็นแผ่นทึบทับตัวเลข
พังเงียบแบบเดียวกับบั๊ก iPhone รอบก่อนเป๊ะ → วาดลงผ้าใบแล้ว**อ่านค่าความโปร่งใสจริง**
ถ้าค่าต่ำสุดกับสูงสุดชนกันแปลว่าโดนทิ้ง = ไม่ผ่าน

**ถามคนอีก 2 ข้อ** ที่เครื่องตอบแทนไม่ได้: เห็นเป็นสีทองไหม · เอียงแล้วขยับไหม
จบด้วยปุ่ม **"คัดลอกผลทดสอบ"** ปุ่มเดียว (มีทางสำรองให้เลือกข้อความเองถ้าคัดลอกไม่ผ่าน)

## พิสูจน์แล้ว

| ตรวจอะไร | ผล |
|---|---|
| เดินครบทุกขั้นแล้วได้รายงานครบทุกหมวด (`demo/_check-mobile.html`) | **21 / 21** |
| ไจโรแปลงสัญญาณเป็นการเอียงถูก (`demo/_check-gyro.html`) | **11 / 11** |
| พอดีจอ ไม่มีอะไรตกจอ — 6 ขนาดเครื่อง × 4 ขั้น | **24 / 24** |
| ชุดทดสอบเดิม | **92 / 92** |

**บั๊กที่เจอจากการวัด ไม่ใช่จากการมอง** (ทั้งหมดแก้แล้ว):
- แนวนอนบนจอเตี้ย **ปุ่มตกจอ 112px** และหน้านี้เลื่อนไม่ได้ = ผู้ทดสอบค้าง
  → เปลี่ยนเป็นสองคอลัมน์ตอนแนวนอน
- แถวกริดกลางเป็น `1fr` ซึ่งย่อต่ำกว่าเนื้อหาไม่ได้ → ดันจนล้น แม้หน้าที่ไม่มีบัตรเลย
- คอลัมน์ของกริดกางตาม**ความกว้างต่ำสุดของบัตร (720px)** ทั้งที่กล่องแม่กว้าง 538px
  → `width:100%` ไปอ้าง 720 แล้วบัตรล้นไปทับคอลัมน์ขวา
- ลิงก์ CSS ผิดชุด (ใช้ `voucher.css` ซึ่งไม่ใช่ตัวที่ดีไซน์นี้ใช้) → บัตรเพี้ยนทั้งใบ

⚠️ **เครื่องมือแคปหน้าจอเรนเดอร์แคบกว่า 500px ไม่ได้** (Chrome บังคับ) ภาพที่แคปที่ 390px
จึงเป็นภาพที่ถูกครอบมาจาก 500px — หลอกให้เห็นว่าบัตรโดนตัด
ความกว้างจริงระดับมือถือต้อง**วัดเอา** ไม่ใช่ดูจากภาพ (วัดแล้วที่ 390 และ 360 ไม่ล้นทั้งคู่)

## เหลืออะไร

ไท้เปิดบนมือถือจริง เดินตามขั้น กดปุ่มคัดลอก แล้วส่งกลับมา
→ ได้ตัวเลขจริงทั้งชุด และเป็นการยืนยันข้อ 1 กับข้อ 2 บนเครื่องจริงไปพร้อมกัน

**จุดเซฟก่อนหน้า `aeff191`** — ถ้าไท้ไม่ผ่าน ย้อนกลับได้เป๊ะด้วยคำสั่งเดียว


---

# ✅ ผลจากเครื่องจริง + สิ่งที่แก้ตาม (2026-08-19)

**iPhone · iOS 18.6 · Safari — ผ่านทั้งสามข้อ**

| | ผล |
|---|---|
| ผิวทอง | ตรวจอัตโนมัติผ่าน · ไท้ยืนยันด้วยตาว่าเป็นสีทอง |
| ไจโรสโคป | live · **8,123 สัญญาณ** · ไท้ยืนยันว่าเอียงแล้วขยับ |
| ความลื่น | กลาง **17 ms** · แย่สุด 19-21 ms · สะดุด 0.8% / 0.3% |
| น้ำหนัก | 710 KB · พร้อมใช้ 1.7 วิ บน 5G |

## สิ่งที่ไท้ทักและแก้แล้ว — เลิกสั่งให้หมุนเครื่อง

เดิมขอให้หมุนเครื่องเป็นแนวนอนก่อนดูใบแนวยาว **ผิด** เพราะ:
พอหมุนเป็นแนวนอน Safari หดพื้นที่จนเหลือเตี้ยมาก · คนส่วนใหญ่ล็อกจอแนวตั้งไว้อยู่แล้ว ·
และแอปบัตรจริง (Thai ID / บัตรเครดิต) ไม่มีใครสั่งให้หมุนเครื่อง เขาหมุน **บัตร** ให้แทน

→ ทำ **โหมดเต็มจอ** เป็นฟีเจอร์จริงในตัวบัตร (`src/ui/card-fullscreen.js` + `card-base.css`)
5 หน้าใช้งานจริงเรียกใช้ได้เลย · ใบแนวยาวหมุน 90° เต็มจอเสมอ · ผู้ใช้เอียงจอเองทีหลัง
แล้วบัตรคืนเป็นแนวปกติให้เอง · ใบกระทัดรัดไม่หมุน

**พิสูจน์แล้ว 14/14** (`demo/_check-fullscreen.html`) รวมข้อสำคัญที่สุดคือ
**แกนการเอียงหมุนตามใบ** — เอียงซ้าย-ขวาแล้วแกนตั้งของใบขยับ 124.2 แกนนอนขยับ 0.4

⚠️ เจอตอนทำ: ใบมีเพดานความกว้าง (`--vk-card-width`) ไว้กันบานบนจอคอม
ในโหมดเต็มจอต้องปลดทิ้ง ไม่งั้นใบยาวไม่ถึงขอบจอ (จอสูง 844 แต่ใบยาวได้แค่ 720)


## รอบที่ไท้เทสไจโรจริงจาก LINE แล้วเจอบั๊ก 2026-08-19

ไท้รันห้องแล็บไจโรจาก LINE (iOS 18.6) แล้วส่งผลกลับมา · **LINE ให้อ่านการเอียงได้จริง**
(7,617 สัญญาณ · 60 ครั้ง/วินาที · 17 ms/เฟรม) — ลบคำเตือนเดิมเรื่องเบราว์เซอร์ในแอปได้

### ของจริงพัง 2 จุด

| อาการที่ไท้เจอ | ต้นเหตุ | วัดก่อน → หลัง |
|---|---|---|
| บัตรค้างเหมือนกำลังเอียง (ถือในมือ) | เกณฑ์ "กำลังขยับ" วัดจากสัญญาณสองตัวติดกัน → มือสั่น 0.15° ที่ 60Hz = 9°/วินาที ประตูปิดตลอด | มือสั่น 0.4°: **ไม่กลับเลย → 1.1 วินาที** |
| หมุนจอกลับเป็นแนวตั้งแล้วบัตรเอียง | สั่ง "จำท่าถือใหม่" ตอนหมุนจอ ซึ่งไปจำตอนมือกำลังหมุนเครื่องอยู่ | ควรได้ 0%: **42.5% → 0.5%** |

ทั้งคู่แก้ที่ `src/ui/foil-tilt.js` ไฟล์เดียว · ระยะที่ประกายกวาดเท่าเดิม (30-34%) ไม่ได้แลกอะไรไป

### หน้าทดสอบเองพัง 3 จุด

- **บัตรหายตอนจอแนวนอน** — สูตรความกว้างติดลบเมื่อจอเตี้ยกว่า 23rem → กว้าง 0
  วัดจริงก่อนแก้: iPhone แนวนอน **18×22 px** · LINE แนวนอน **0×0** → หลังแก้ 210×256 / 161×196
  (และตัวเอียงข้ามใบขนาด 0 ทิ้ง → ข้อแนวนอนเลยได้ 0% เหมือนของจริงพัง)
- **ข้อ "ถือนิ่ง" ตัดสินจากองศาดิบ** ตกทั้งที่ประกายไหวแค่ 0.2% → เปลี่ยนไปตัดสินจากประกาย
- **ข้อเต็มจอวัดหัวเทียบท้าย** ได้ 18/24% ต่ำกว่าจริง → วัดช่วงกว้างสุดแทน

### เพิ่ม: เลขรุ่นบนหน้าทดสอบ

ไท้เทสของเก่าที่ค้างในแคช LINE ไปทั้งรอบโดยไม่มีใครรู้ (จอบอก 14 ข้อ แต่ที่ปล่อยไปมี 13 ข้อ)
→ หน้าแล็บโชว์ **เลขรุ่น** บนจอ + ในรายงาน และต่อ `?v=` ท้ายไฟล์โค้ดทุกตัวที่ดึง

### ตัวขับทดสอบเองก็พัง

ที่รายงาน "ประกายกวาดแนวนอน 0%" มาหลายรอบ **เป็นบั๊กของตัวขับ ไม่ใช่ของโค้ดจริง**
มันอ่านว่า "ตอนนี้ข้อไหน" ก่อนแล้วค่อยรอปุ่ม พอแล็บเดินไปข้อถัดไปเองระหว่างนั้น
ก็ไปรอปุ่มที่ไม่มีวันโผล่ 20 วินาที แล้ว **ไม่เอียงเครื่องเลยทั้งข้อ**
แก้เป็น "รอให้ UI ขอก่อน แล้วค่อยอ่านว่าข้อไหน" → **19/19 · แล็บเองผ่าน 13/13**


## รอบแก้ที่สอง 2026-08-21 — ประตู "กำลังเอียงอยู่" ต้องนุ่ม

ผลเทสของไท้จาก LINE หลังรอบแรก **ผ่าน 12/14** · ข้อจอแนวนอนจาก 0% เป็น **79%** (บัตรไม่หายแล้ว) ·
ถือนิ่งผ่าน · เอียงซ้าย-ขวา 125% · เงย-ก้ม 123% — **แต่ "ยกขึ้นถือใหม่" ยังค้างที่ 14 องศาหลัง 12 วินาที**

รอบแรกแก้ถูกทิศ (เกลี่ยสัญญาณก่อนวัดความเร็ว) แต่ไม่พอ เพราะตัวประตูเองยังเป็นแบบ **เปิด-ปิดเด็ดขาด**
`spd > 9 องศา/วินาที = หยุดไล่ศูนย์` ซึ่ง **มือคนถือมือถือแกว่งเกิน 9 องศา/วินาทีเป็นเรื่องปกติ**
→ ประตูปิดเกือบตลอด → พอบวกมือที่ไหลช้า ๆ ค่าไปค้างที่จุดสมดุลแล้วไม่ขยับอีก

### ทำซ้ำได้บนเครื่องพัฒนาก่อนแก้ (สำคัญที่สุดของรอบนี้)

สัญญาณสังเคราะห์แบบเนียนสนิทจับบั๊กนี้ไม่ได้เลยสักรอบ · ต้องมี **แบบจำลอง "มือคน"**:
ท่าถือ + สั่น 3 ความถี่ (3.1 / 7.3 / 11.7 Hz) + มือไหลช้าเชิงเส้น 1.5 องศา/วินาที
ใส่เข้าไปแล้ว **ค้างที่ ~20% ตลอด 12 วินาที = อาการเดียวกับที่ไท้เจอเป๊ะ**

### ทางแก้ + ตัวเลข (เทียบ 4 ชุดค่า เลือกชุดที่ดีสุด)

ตัวคูณนุ่ม `calm = 1/(1+(spd/12)²)` คูณเข้ากับความเร็วไล่ศูนย์ — **ไม่มีวันเป็น 0 = ไม่มีวันค้างถาวร**
พร้อมเร่งความเร็วไล่เป็น 0.20 / 3.00 ต่อวินาที

| | ของเดิม | หลังแก้ |
|---|---|---|
| ยกขึ้นถือ 12 วินาที | ค้างที่ 8 | **3** |
| มือไหลช้า | **ค้าง 21 ไม่ลงเลย** | **14 และลงเรื่อย ๆ** |
| เอียงปกติ | 129% | 126% |
| เอียงช้ามาก (3-4 วิ/ข้าง) | 115% | **82%** ← ของที่ยอมแลก |

**ยอมแลก:** เอียงช้ามาก ๆ ระบบจะเริ่มนับว่าเป็นท่าถือใหม่ ประกายกวาดสั้นลง
คุ้มกว่า เพราะอาการค้างเจอทุกครั้ง แต่เอียงช้าขนาดนั้นแทบไม่มีใครทำ

### หน้าแล็บ

ข้อ "เต็มจอ" เดิมรอเฉพาะการเอียงแกนซ้าย-ขวา — เอียงคนละแกนแล้วมันนั่งรอจนหมดเวลา
30 วินาที แล้วรายงาน 0% เหมือนของจริงพัง (ไท้เจอ) → **รับแกนไหนก็ได้ + บอกตรง ๆ เมื่อไม่ได้เอียงจริง**
